# ROADMAP

OneApp 的功能开发规划（backlog）。本文件只记录**待开发**的功能与顺序；
真正动手某个功能时，再用 OpenSpec 流程为它建 change（`/opsx:propose`），
完成并发布后在此勾选并同步 `CHANGELOG.md`。

**约定：每次只新增一个功能，一条分支一个功能，按下方顺序推进。**

状态图例：`[ ]` 待开发 · `[~]` 进行中 · `[x]` 已完成

---

## P0 · 地基（优先做，解锁后续所有文本类工具）

- [x] **统一编辑器** —— 合并 Markdown / HTML 两个编辑器为一个 `EditorTab`，
  按文件后缀自动识别编辑/预览方式。（v1.5.0 已完成）
  > 纯文本类型扩展（`.txt/.css/.js/.xml`…）作为后续增量跟进。

## P1 · 下一阶段优先（现有体验补强 + 高频低风险工具）

- [x] **正则测试器** —— 实时高亮匹配、分组、常用速查（v1.6.0 已完成）
- [x] **编码转换合集** —— Base64 / URL encode·decode / JWT 解码 / Hash(MD5·SHA) / 进制转换 / Unicode·Emoji 转义（v1.7.0 已完成）
- [x] **Agent 研讨室** —— 多个本地 AI agent 只读研讨本地仓库，交叉评审后汇总实现方案（v1.8.0 已完成）
- [x] **Agent 研讨室记录导航** —— 将左侧「进度」模块作为轻量目录使用：（v1.11.0 已完成）
  点击某一轮次下的 Agent chip（如「第一轮 · Codex」）时，右侧「研讨记录」区域快速滚动到对应轮次、对应 Agent 的消息位置，并短暂高亮目标消息，方便在长记录中定位与回看。
  - 交互方案：仅对已产生消息的 chip 启用点击与 hover 状态；`pending` / `running` / 无对应消息的状态保持展示用途，避免误导。
  - 实现边界：优先在 `AgentWorkshopTab.vue` 渲染层建立 `phase + agentId` 到消息 DOM 的锚点映射，不改主进程 IPC、研讨编排、记录持久化格式和 Agent 调用逻辑。
  - 后续扩展：若记录继续变长，可再评估独立目录、当前阅读位置同步、错误消息跳转等增强。
- [x] **YAML ⇆ JSON 转换** —— 复用现有 JSON 工具的校验/错误定位架构（v1.12.0 已完成）
- [x] **文本处理合集** —— 统计 / 大小写转换 / 排序 / 去重（v1.13.0 已完成）
- [x] **生成器合集一期** —— 新建「生成工具」分组，提供 UUID v4 单个/批量生成、随机密码生成、Lorem 占位文本生成。（v1.14.0 已完成）
  - UUID：支持单个和批量生成，每行一个 v4 UUID。
  - 随机密码：支持长度、大小写、数字、符号、排除易混字符。
  - Lorem：支持按词 / 句 / 段生成。
  - 不做范围：UUID v7、密码强度评分、密码短语、生成历史、二维码。

## P2 · 中期推进（价值明确，涉及更多集成或格式细节）

- [ ] **检查更新与弹窗图标统一** —— 将设置页「检查更新」从固定 `alert` 文案改为真实检查 GitHub Releases 最新版本，并统一消息弹窗使用 OneApp 应用图标。
  - 检查更新方案：点击按钮后进入 loading 状态，请求 `kaituoC/OneApp` 最新 GitHub Release，比较当前 `__APP_VERSION__` 与最新 `tag_name`；无新版时提示「已是最新版本」，有新版时展示版本号、发布日期、更新说明摘要，并提供「前往下载 / 打开 Release 页面」入口。
  - 弹窗方案：新增主进程侧统一消息弹窗 IPC，由 `dialog.showMessageBox` 显示信息、确认等消息，并默认注入 `electron/assets/icon.png`（生产环境使用打包后的 `assets/icon.png`）；替换设置页 `alert`，并同步迁移 Agent 研讨室成本提示等 renderer 原生 `confirm` 弹窗。
  - 不做范围：暂不接入 `electron-updater`，不自动下载、不自动安装、不提示重启更新；完整自动更新待应用签名与 Release metadata 发布链路就绪后，另起独立需求评估。
- [ ] **数据工具合集一期：CSV ⇆ JSON / 表格预览** —— 将现有 JSON / YAML 入口升级为「数据工具」合集，内部保留 JSON / YAML 子工具，并新增 CSV 子工具。
  - CSV → JSON：输出对象数组，每行对象以表头作为 key。
  - JSON → CSV：支持对象数组，字段来自所有对象 key 的并集。
  - 表格预览：只读展示解析后的二维表，支持横向滚动。
  - 解析规则：引入成熟轻量 CSV 库，支持双引号包裹、字段内逗号、字段内换行和双引号转义。
  - 不做范围：单元格编辑、排序筛选、文件导入导出、大文件虚拟滚动。
- [ ] **数据工具合集二期：SQL / XML 格式化** —— 在「数据工具」合集内新增 SQL / XML 轻量格式化子工具。
  - SQL：格式化、压缩。
  - XML：格式化、压缩。
  - 错误提示：仅覆盖明显结构问题。
  - 不做范围：SQL 方言语义校验、数据库连接、XML Schema / DTD 校验、XPath。
- [ ] **JSON 工具增强：JSONPath 查询** —— 在数据工具内提供 JSONPath 查询能力。
  - 展示匹配数量、结果路径和值摘要列表，并输出完整匹配结果 JSON。
  - 错误提示：JSON 无效、JSONPath 无效、无匹配。
  - 不做范围：原 JSON 树高亮、批量修改、可视化路径选择器。
- [ ] **时间工具增强一期：Cron 表达式解释器** —— 并入现有时间工具，支持标准 5 位 Cron。
  - 格式：`分钟 小时 日 月 星期`。
  - 使用本地时区，展示人类可读解释和未来 5 次执行时间。
  - 不做范围：秒字段、Quartz 年份字段、时区选择。

## P3 · 后续评估（使用场景更窄或可独立排期）

- [ ] **生成器增强：二维码生成** —— 并入「生成工具」分组，提供文本 / URL 二维码基础生成。
  - 支持尺寸、纠错级别、下载 PNG、复制 PNG。
  - 不做范围：Logo、颜色、圆角等品牌样式、图片识别 / 扫码解析。
- [ ] **时间工具增强二期：多时区对照表** —— 并入现有时间工具，提供当前时间对照。
  - 预置常用时区 / 城市，可添加、移除城市。
  - 实时显示当前时间和日期差异，例如今天 / 明天 / 昨天。
  - 不做范围：会议时间换算、办公时间重叠分析。

---

> 优先级排序依据：使用频率、实现成本、与现有架构的契合度、以及是否为后续功能解锁地基。
> 顺序可随实际需求调整；新点子先加到对应优先级分组的末尾。
> 已确认的剩余功能开发队列见 `docs/roadmap/2026-07-01-remaining-feature-roadmap-design.md`。
