## REMOVED Requirements

### Requirement: HTML file listing
**Reason**: 合并为统一编辑器后，HTML 文件浏览统一由 `file-tree-explorer` 的目录树承担，按当前 `mode` 过滤显示 `.html`/`.htm`，不再有独立的 HTML 文件列表能力。
**Migration**: 见 `file-tree-explorer` 的 "Type-based file filtering in tree"。
